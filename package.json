{
  "name": "email-consumer-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "queue:test": "node scripts/queue-smoke-test.mjs"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "*",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8",
    "wrangler": "^4.5.0"
  }
}
